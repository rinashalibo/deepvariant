Step one of DeepVariant: creates tf.Example protos for training/calling.
flags:

/tmp/Bazel.runfiles_bv5zcmqu/runfiles/com_google_deepvariant/deepvariant/make_examples.py:
  --[no]add_hp_channel: (DEPRECATED) Use `channel_list` to add this channel.

    If true, add another channel to represent HP tags per read.
    (default: 'false')
  --[no]add_supporting_other_alt_color: If True, reads supporting an alt not
    represented in the pileup image are colored differently for multiallelics.
    (default: 'false')
  --alt_aligned_pileup: <none|base_channels|diff_channels|rows|single_row>:
    Include alignments of reads against each candidate alternate allele in the
    pileup image. "none" turns this feature off. The default is "none". Options:
    "none", "base_channels", "diff_channels", "rows", and "single_row".
    (default: 'none')
  --[no]assign_phase_from_normal: If True, in a DeepSomatic run, use phase
    information from the normal sample to assign phase to the tumor sample.
    (default: 'false')
  --aux_fields_to_keep: (DEPRECATED); AUX field parsing is controlled based on
    other flags.
    HP is parsed if --phase_reads=False and --sort_by_haplotypes,
    --reverse_haplotypes, or --hp_tag_for_assembly_polishing are set.
    HP is parsed if --phase_reads=False and the 'haplotype' channel is
    requested.
    HP is parsed if --output_phasing_error_stats is set.
    OQ is parsed if --use_original_quality_scores is set.
    MM, ML, MN are parsed if the "base_methylation" channel is requested or
    if --enable_methylation_calling or if --enable_methylation_aware_phasing
    is set.
    (default: '')
  --[no]call_small_model_examples: If True, use a smaller model to call SNPs.
    (default: 'false')
  --candidate_positions: Path to the binary file containing candidate positions.
  --candidates: Candidate DeepVariantCalls in tfrecord format. For DEBUGGING.
    (default: '')
  --channel_list: Comma or space-delimited list of channels to create examples
    for. Channels are created in the order specified by this flag. You can use
    BASE_CHANNELS which will be substituted for six commonly used channels:

    read_base,base_quality,mapping_quality,strand,read_supports_variant,base_dif
    fers_from_ref

    All available channels:
    - read_base
    - base_quality
    - mapping_quality
    - strand
    - read_supports_variant
    - base_differs_from_ref
    - haplotype
    - allele_frequency
    - read_mapping_percent
    - avg_base_quality
    - identity
    - gap_compressed_identity
    - gc_content
    - is_homopolymer
    - homopolymer_weighted
    - blank
    - insert_size
    - mean_coverage
    - base_methylation
    - base_6ma
    - read_supports_variant_fuzzy
    - supplementary_alignment
    - allele_sample_probability
    - homopolymer_insertion_quality
    - homopolymer_deletion_quality
    - inter_homopolymer_insertion_quality

  --channels: (DEPRECATED) Use `channel_list` flag instead
  --checkpoint: Path to the TensorFlow model checkpoint.
  --checkpoint_json: Optional. File path to a model.example_info.json to pair
    with --checkpoint. If not set, we will use the model.example_info.json found
    in the checkpoint directory.
  --confident_regions: Regions that we are confident are hom-ref or a variant in
    BED format. In BED or other equivalent format, sorted or unsorted. Contig
    names must match those of the reference genome.
    (default: '')
  --[no]create_complex_alleles: If True, complex alleles are enabled.
    (default: 'false')
  --customized_classes_labeler_classes_list: A comma-separated list of strings
    that defines customized class labels for variants. This is only set when
    labeler_algorithm is customized_classes_labeler.
    (default: '')
  --customized_classes_labeler_info_field_name: The name from the INFO field of
    VCF where we should get the customized class labels from. This is only set
    when labeler_algorithm is customized_classes_labeler.
    (default: '')
  --denovo_regions: Regions where variants are de novo. Used to label variants
    as de novo.
    (default: '')
  --[no]deterministic_serialization: If True, the saved protos will be run with
    SerializeToString(deterministic=True). This is useful for creating
    deterministic testdata.
    (default: 'false')
  --[no]discard_non_dna_regions: Default is False. If set regions of Ns larger
    than 300,000bp arediscarded.
    (default: 'false')
  --downsample_classes: If set, downsample training examples for each specified
    class. For example, if set to 0.5,0.5,1.0 then class 0 and 1 will be
    downsampled to 50% of the original number of examples, and class 3 will be
    left unchanged. This flag is only used in training mode.
    (default: '')
    (a comma separated list)
  --downsample_fraction: If not 0.0 must be a value between 0.0 and 1.0. Reads
    will be kept (randomly) with a probability of downsample_fraction from the
    input BAM. This argument makes it easy to create examples as though the
    input BAM had less coverage.
    (default: '0.0')
    (a number)
  --[no]enable_joint_realignment: If True, realign reads from all samples
    together. By default this is False, which means reads from each sample are
    realigned per-sample.
    (default: 'false')
  --[no]enable_methylation_aware_phasing: [Experimental] If True, enables
    methylation-aware phasing. This mode uses methylation information to phase
    reads. With this option, the p-value for allele-specific methylation is
    output as format field MI.
    Note: This feature is experimental and may change in future versions.
    (default: 'false')
  --[no]enable_methylation_calling: If True, then methylation calling mode will
    be enabled. This mode uses methylation calling threshold to determine if a
    base is methylated.
    (default: 'false')
  --[no]enable_strict_insertion_filter: If True, add stricter filter for
    insertion in window selector. (experimental)
    (default: 'false')
  --examples: Required. Path to write tf.Example protos in TFRecord format.
  --exclude_contigs_for_methylation_phasing: List of chromosomes to exclude from
    methylation-aware phasing. For all listed chromosomes, methylation-aware
    phasing will be skipped. The list can be either comma- or space-separated.
    (default: 'chrX chrY')
  --exclude_regions: Optional. Space-separated list of regions we want to
    exclude from processing. Elements can be region literals (e.g., chr20:10-20)
    or paths to BED/BEDPE files. Region exclusion happens after processing the
    --regions argument, so --region 20 --exclude_regions 20:100 does everything
    on chromosome 20 excluding base 100
    (default: '')
  --exclude_variants_af_threshold: Only used when exclude_variants_vcf_filename
    is set. We exclude candidates with alts below this threshold.
    (default: '0.05')
    (a number)
  --exclude_variants_vcf_filename: Optional. Population VCF (with AF) to exclude
    variants from. In our use case, this is currently only used for DeepSomatic
    tumor-only training examples creation.
  --gvcf: Optional. Path where we should write gVCF records in TFRecord of
    Variant proto format.
    (default: '')
  --gvcf_gq_binsize: Bin size in which to quantize gVCF genotype qualities.
    Larger bin size reduces the number of gVCF records at a loss of quality
    granularity. Must be a positive integer.
    (default: '5')
    (an integer)
  --haploid_contigs: Optional list of non autosomal chromosomes. For all listed
    chromosomesHET probabilities are not considered. The list can be either
    comma or space-separated.
  --hp_tag_for_assembly_polishing: If set to > 0, reads with this HP tag will be
    sorted on top. sort_by_haplotypes has to be set to True for this to work.
    (default: '0')
    (an integer)
  --hts_block_size: Sets the htslib block size. Zero or negative uses default
    htslib setting; larger values (e.g. 1M) may be beneficial for using remote
    files. Currently only applies to SAM/BAM reading.
    (default: '134217728')
    (an integer)
  --[no]include_med_dp: If true, include MED_DP in the output gVCF records.
    (default: 'false')
  --[no]keep_duplicates: If True, keep duplicate reads.
    (default: 'false')
  --[no]keep_legacy_allele_counter_behavior: If True, the behavior in this
    commit is reverted: https://github.com/google/deepvariant/commit/fbde0674639
    a28cb9e8004c7a01bbe25240c7d46. We do not recommend setting this flag to
    True.
    (default: 'false')
  --[no]keep_secondary_alignments: If True, keep reads marked as secondary
    alignments.
    (default: 'false')
  --[no]keep_supplementary_alignments: If True, keep reads marked as
    supplementary alignments.
    (default: 'false')
  --labeler_algorithm: Algorithm to use to label examples in training mode. Must
    be one of the LabelerAlgorithm enum values in the MakeExamplesOptions proto.
    (default: 'haplotype_labeler')
  --logging_every_n_candidates: Print out the log every n candidates. The
    smaller the number, the more frequent the logging information emits.
    (default: '2000')
    (an integer)
  --max_read_length_to_realign: The maximum length of a read to realign. Reads
    longer than this length are never realigned.
    (default: '500')
    (an integer)
  --max_reads_for_dynamic_bases_per_region: If > 0, set the max number of bases
    to (max_reads_for_dynamic_bases_per_region * region length).This is
    particularly important for very long reads.
    (default: '0')
    (an integer)
  --max_reads_per_partition: The maximum number of reads per partition that we
    consider before following processing such as sampling and realigner.
    (default: '1500')
    (an integer)
  --mean_coverage_per_sample: Optional. The mean number of reads aligned to any
    given position or base in the regions of interest. Used in CH_MEAN_COVERAGE
    channel. If this value is unset an approximation will be used using an
    online streaming algorithm for the mean coverage channel.
    (default: '')
    (a comma separated list)
  --methylation_calling_threshold: Sets the minimum threshold for a base to be
    considered methylated.This is used in methylation calling mode.
    (default: '0.5')
    (a number)
  --min_alleles_to_phase: The minimum number of alleles on a read that must
    support a phase to be assigned that phase.
    (default: '1')
    (an integer)
  --min_base_quality: Minimum base quality. This field indicates that we are
    enforcing a minimum base quality score for alternate alleles. Alternate
    alleles will only be considered if all bases in the allele have a quality
    greater than min_base_quality.
    (default: '10')
    (an integer)
  --min_mapping_quality: Setting this field to a positive integer i will only
    keep reads thathave a MAPQ >= i. Note this only applies to aligned reads.
    (default: '5')
    (an integer)
  --mode: Mode to run. Must be one of calling, training or candidate_sweep.
    calling - examples are prepared for inference only. training - examples are
    prepared with labels for training. candidate_sweep - (advanced pre-step) -
    candidate positions are    prepared for the subsequent run of make_examples
    with intervals    created with equal number of candidates.   NOTE: When this
    option is used, make_examples must be run again          with the mode set
    to calling.
    (default: '')
  --multi_allelic_mode: How to handle multi-allelic candidate variants. For
    DEBUGGING
    (default: '')
  --non_uniform_downsampling_threshold: The threshold for the non-uniform
    downsampling. If the number of reads representing an allele is less than
    this threshold, the reads are not downsampled.
    (default: '3')
    (an integer)
  --[no]normalize_reads: If True, allele counter left align INDELs for each
    read.
    (default: 'false')
  --[no]output_debug_info: If True, output debug info to the log.
    (default: 'false')
  --output_local_read_phasing: [optional] For debugging only. If filename ends
    in .tsv, output a TSV containing read phases. Alternatively, if filename
    ends in .bam, output a phased BAM. If examples are sharded, this should be
    sharded into the same number of shards as the examples.
  --[no]output_phase_info: If True, output phase information to the candidates.
    (default: 'false')
  --output_phasing_error_stats: [optional] For debugging only. If filename ends
    in .tsv, output a TSV containing phasing error stats. If examples are
    sharded, this should be sharded into the same number of shards as the
    examples.
  --[no]output_sitelist: If True, output a list of sites present in examples
    output.
    (default: 'false')
  --p_error: Basecalling error for reference confidence model.
    (default: '0.001')
    (a number)
  --par_regions_bed: Optional BED file containing Human Pseudoautosomal Region
    (PAR) regions.Variants within this region are unaffected by genotype
    reallocation applied on regions supplied by --haploid_contigs flag.
  --[no]parse_sam_aux_fields: (DEPRECATED); AUX field parsing is controlled
    based on other flags.
    HP is parsed if --phase_reads=False and --sort_by_haplotypes,
    --reverse_haplotypes, or --hp_tag_for_assembly_polishing are set.
    HP is parsed if --phase_reads=False and the 'haplotype' channel is
    requested.
    HP is parsed if --output_phasing_error_stats is set.
    OQ is parsed if --use_original_quality_scores is set.
    MM, ML, MN are parsed if the "base_methylation" channel is requested or
    if --enable_methylation_calling or if --enable_methylation_aware_phasing
    is set.
  --partition_size: The maximum number of basepairs we will allow in a region
    before splitting it into multiple smaller subregions.
    (default: '1000')
    (an integer)
  --phase_max_candidates: Limits the number of candidates for phasing. If number
    of candidates exceeds the maximum then phasing is not performed for the
    window. This flag is used only when phase_reads is true.
    (default: '5000')
    (an integer)
  --[no]phase_reads: Calculate phases and add HP tag to all reads on a fly.
    --phase_reads requires --track_ref_reads=True.
    (default: 'false')
  --pileup_image_height: Height for the pileup image. If 0, uses the default
    height
    (default: '0')
    (an integer)
  --pileup_image_width: Width for the pileup image. If 0, uses the default width
    (default: '0')
    (an integer)
  --population_vcfs: Optional. Tabix-indexed VCF file (or list of VCFs broken by
    chromosome), separated by comma or space, containing population allele
    frequencies. Each of the item can be a file path, or a wildcard pattern.
  --proposed_variants: (Only used when --variant_caller=vcf_candidate_importer.)
    Tabix-indexed VCF file containing the proposed positions and alts for
    `vcf_candidate_importer`. The GTs will be ignored.
    (default: '')
  --reads: Required. Aligned, sorted, indexed BAM file containing the reads we
    want to call. Should be aligned to a reference genome compatible with --ref.
    Can provide multiple BAMs (comma-separated).
  --[no]realign_reads: If True, locally realign reads before calling variants.
    Reads longer than max_read_length_to_realign bp are never realigned.
    (default: 'true')
  --ref: Required. Genome reference to use. Must have an associated FAI index as
    well. Supports text or gzipped references. Should match the reference used
    to align the BAM file provided to --reads.
  --regions: Optional. Space-separated list of regions we want to process.
    Elements can be region literals (e.g., chr20:10-20) or paths to BED/BEDPE
    files.
    (default: '')
  --[no]reverse_haplotypes: If True, reads are sorted by haplotypes in reverse
    order.
    (default: 'false')
  --runtime_by_region: [optional] Output filename for a TSV file of runtimes and
    other stats by region. If examples are sharded, this should be sharded into
    the same number of shards as the examples.
  --[no]sample_mean_coverage_on_calling_regions: Optional. If True, the mean
    coverage is calculated on the calling regions, rather than on the whole
    genome. This is useful in the case of WES where the regions that have reads
    are 2 percent of the genome.
    (default: 'false')
  --sample_name: Sample name to use for our sample_name in the output
    Variant/DeepVariantCall protos. If not specified, will be inferred from the
    header information from --reads.
    (default: '')
  --select_variant_types: If provided, should be a whitespace-separated string
    of variant types to keep when generating examples. Permitted values are
    "snps", "indels", "multi-allelics", and "all", which select bi-allelic snps,
    bi-allelic indels, multi-allelic variants of any type, and all variants,
    respectively. Multiple selectors can be specified, so that
    --select_variant_types="snps indels" would keep all bi-allelic SNPs and
    indels
  --sequencing_type: A string representing input bam file sequencing_type.
    Permitted values are "WGS" and "WES", which represent whole genome
    sequencing and whole exome sequencing, respectively. This flag is
    experimental and is not currently being used.
  --shm_buffer_size: shared memory files buffer size.
    (default: '10485760')
    (an integer)
  --shm_prefix: shared memory files prefix.
    (default: '')
  --[no]skip_pileup_image_generation: If True, skips generating pile up images.
    Should be set only if write_small_model_examples is true.
    (default: 'false')
  --[no]small_model_call_multiallelics: Allows the small model to call
    multiallelic candidate sites.
    (default: 'true')
  --[no]small_model_emit_all_candidates: [Experimental] When this flag is set,
    the small model classifies all candidates while also passing them along to
    the CNN for classification. These are resolved during postprocess_variants
    into separate VCF files per GQ threshold. The purpose is to explore the
    joint accuracy of the two models as a function of GQ thresholds.
    (default: 'false')
  --small_model_indel_gq_threshold: Sets the minimum threshold for GQ scores for
    the small model for INDELs. Set to -1 to disable calling INDELs with the
    small model.
    (default: '0')
    (an integer)
  --small_model_inference_batch_size: Sets the batch size used by the small
    model during inference.
    (default: '128')
    (an integer)
  --small_model_snp_gq_threshold: Sets the minimum threshold for GQ scores for
    the small model for SNPs. Set to -1 to disable calling SNPs with the small
    model.
    (default: '0')
    (an integer)
  --small_model_vaf_context_window_size: Sets the window size used by small
    model to produce context VAF allele counts.
    (default: '51')
    (an integer)
  --[no]sort_by_alt_allele_support: If True, reads in the pileup image will be
    sorted primarily by the alternate allele they support (from the variant
    call), then by haplotype, then by position. Reads supporting reference or
    unlisted alts will appear after reads supporting listed alts.
    (default: 'false')
  --[no]sort_by_haplotypes: If True, reads are sorted by haplotypes.
    (default: 'false')
  --[no]stream_examples: If True, stream examples to shared memory buffer.
    (default: 'false')
  --task: Task ID of this task
    (default: '0')
    (an integer)
  --[no]track_ref_reads: If True, allele counter keeps track of ref supporting
    reads.By default allele counter keeps a simple count of number of reads
    supporting ref.
    (default: 'false')
  --trained_small_model_path: Path to a small model checkpoint directory.
    (default: '')
  --training_random_emit_ref_sites: If > 0, emit extra random reference examples
    with this probability.
    (default: '0.0')
    (a number)
  --[no]trim_reads_for_pileup: If True, trim reads that are used to build a
    pileup image. This flag will be set to True if --alt_aligned_pileup is not
    none.
    (default: 'false')
  --truth_variants: Tabix-indexed VCF file containing the truth variant calls
    for this labels which we use to label our examples.
    (default: '')
  --types_to_alt_align: <indels|all>: When --alt_aligned_pileup is not none,
    this flag determines whether to align to the alt alleles only for indels or
    for all variant types including SNPs. Ignored if --alt_aligned_pileup is
    "none". This flag is experimental and is not compatible with the pre-trained
    release models.
    (default: 'indels')
  --[no]use_allele_frequency: (DEPRECATED) Use `channel_list` to add this
    channel.

    If True, add another channel for pileup images to represent allele frequency
    information gathered from population callsets.
    (default: 'false')
  --[no]use_non_uniform_downsampling: If True, reads are downsampled using a
    non-uniform distribution. This is useful for DeepSomatic, where we want to
    downsample the normal reads more aggressively than the tumor reads.
    (default: 'false')
  --[no]use_original_quality_scores: If True, base quality scores are read from
    OQ tag.
    (default: 'false')
  --[no]use_ref_for_cram: If true, use the --ref argument as the reference file
    for the CRAM file passed to --reads.  In this case, it is required that the
    reference file be located on a local POSIX filesystem. To disable, specify
    --nouse_ref_for_cram.
    (default: 'true')
  --[no]use_rejected_alleles: If True, use read support information from
    rejected alleles in pileup.This flag is only used when
    read_supports_variant_fuzzy channel is enabled.
    (default: 'false')
  --variant_caller: The caller to use to make examples. Must be one of the
    VariantCaller enum values in the MakeExamplesOptions proto.
    (default: 'very_sensitive_caller')
  --vsc_max_fraction_indels_for_non_target_sample: In candidate generation, if
    any non-target sample has more Indels than this threshold, the candidate
    will be excluded.Default is 0.0 which means no max is set.
    (default: '0.0')
    (a number)
  --vsc_max_fraction_snps_for_non_target_sample: In candidate generation, if any
    non-target sample has more SNPs than this threshold, the candidate will be
    excluded.Default is 0.0 which means no max is set.
    (default: '0.0')
    (a number)
  --vsc_min_count_indels: Indel alleles occurring at least this many times in
    our AlleleCount will be advanced as candidates.
    (default: '2')
    (an integer)
  --vsc_min_count_snps: SNP alleles occurring at least this many times in our
    AlleleCount will be advanced as candidates.
    (default: '2')
    (an integer)
  --vsc_min_fraction_indels: Indel alleles occurring at least this fraction of
    all counts in our AlleleCount will be advanced as candidates.
    (default: '0.06')
    (a number)
  --vsc_min_fraction_multiplier: In candidate generation, this multiplier is
    applied to the minimum allele fraction thresholds (vsc_min_fraction_snps and
    vsc_min_fraction_indels) to adapt thresholds for multi-sample calling. This
    has to in the (0, 1] interval. It can also be set to float("inf")
    programmatically to only use candidates from the target sample in multi-
    sample calling.
    (default: '1.0')
    (a number)
  --vsc_min_fraction_snps: SNP alleles occurring at least this fraction of all
    counts in our AlleleCount will be advanced as candidates.
    (default: '0.12')
    (a number)
  --vsc_min_indel_fraction_for_large_indels: If > 0, this flag along with
    vsc_min_indel_fraction_for_small_indels and vsc_small_indel_threshold
    determines allele fraction for indels.
    (default: '0.0')
    (a number)
  --vsc_min_indel_fraction_for_small_indels: If > 0, this flag along with
    vsc_min_indel_fraction_for_large_indels and vsc_small_indel_threshold
    determines allele fraction for indels.
    (default: '0.0')
    (a number)
  --vsc_small_indel_threshold: If > 0, this flag along with
    vsc_min_indel_fraction_for_small_indels and
    vsc_min_indel_fraction_for_large_indels determines allele fraction for
    indels.
    (default: '0')
    (an integer)
  --[no]write_run_info: If True, write out a MakeExamplesRunInfo proto besides
    our examples in text_format.
    (default: 'false')
  --[no]write_small_model_examples: If True, write small model training examples
    to .tfrecord files.
    (default: 'false')

Try --helpfull to get a list of all flags.
